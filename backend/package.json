{
  "name": "backend",
  "version": "1.0.0",
  "description": "This backend serves a simple API that returns city data to the frontend. The city data is stored statically in a JSON file and is served via an Express server.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1"
  }
}
